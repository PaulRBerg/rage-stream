import { BigNumber } from "@ethersproject/bignumber";

export const RATE_PER_SECOND: BigNumber = BigNumber.from(1);
export const STREAM_DEPOSIT: BigNumber = BigNumber.from(3600);
export const TIME_DELTA: BigNumber = BigNumber.from(3600); // 3600 seconds
export const TIME_OFFSET: BigNumber = BigNumber.from(300); // 300 seconds
